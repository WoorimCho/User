# Build output / caches (rebuilt inside the image)
.gradle
build
**/build/

# IDE / OS
.idea
*.iml
.vscode
.DS_Store
Thumbs.db

# VCS
.git
.gitattributes
.gitignore

# Docs
*.md

# Docker (no Docker-in-Docker)
Dockerfile
.dockerignore
compose.yaml
